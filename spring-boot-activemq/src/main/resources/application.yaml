server:
  port: 8888
spring:
  activemq:
    broker-url: nio://localhost:61608
    user: admin
    password: admin
  jms:
    pub-sub-domain: true #false queue true topic
queue:
  queueName: boot-activemq-queue
topic:
  topicName: boot-active-topic
