server:
  port: 8888
spring:
  activemq:
    broker-url: tcp://localhost:61616
    user: admin
    password: admin
  jms:
    pub-sub-domain: true #false queue true topic
  profiles:
    active: home
queue:
  queueName: boot-activemq-queue
topic:
  topicName: boot-active-topic